# Frontend environment variables
# Set this to your backend API base URL (Laravel routes/api.php -> /api/v1)
# For local development with `php artisan serve` (default 8000):
# VITE_API_BASE_URL=http://localhost:8000/api/v1
